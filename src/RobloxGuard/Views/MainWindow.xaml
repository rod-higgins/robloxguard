<Window x:Class="RobloxGuard.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RobloxGuard — Network &amp; Script Monitor"
        Width="1100" Height="700"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <Window.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="#313244" CornerRadius="6,6,0,0" Padding="16,8" Margin="2,0">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#45475A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#181825"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="BorderBrush" Value="#313244"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#313244"/>
            <Setter Property="RowBackground" Value="#1E1E2E"/>
            <Setter Property="AlternatingRowBackground" Value="#181825"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#A6ADC8"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderBrush" Value="#45475A"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#313244" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="🛡" FontSize="22" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="RobloxGuard" Foreground="#CDD6F4" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    <TextBlock x:Name="RobloxStatusText" Foreground="#A6ADC8" FontSize="13" VerticalAlignment="Center" Margin="20,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="StartButton" Content="▶ Start Monitoring" Click="StartButton_Click"
                            Background="#A6E3A1" Foreground="#1E1E2E" FontWeight="Bold"
                            Padding="16,8" Margin="4,0" BorderThickness="0" Cursor="Hand"/>
                    <Button x:Name="StopButton" Content="⏹ Stop" Click="StopButton_Click"
                            Background="#F38BA8" Foreground="#1E1E2E" FontWeight="Bold"
                            Padding="16,8" Margin="4,0" BorderThickness="0" Cursor="Hand" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content -->
        <TabControl Grid.Row="1" Background="#1E1E2E" BorderThickness="0" Margin="8">

            <!-- Network tab -->
            <TabItem Header="🌐 Network Connections">
                <Grid Margin="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Stats bar -->
                    <Border Grid.Row="0" Background="#313244" CornerRadius="6" Padding="12,8" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Foreground="#A6ADC8" VerticalAlignment="Center">
                                <Run Text="Total: "/><Run x:Name="TotalConnectionsRun" Text="0" FontWeight="Bold" Foreground="#CDD6F4"/>
                                <Run Text="    Suspicious: "/><Run x:Name="SuspiciousConnectionsRun" Text="0" FontWeight="Bold" Foreground="#F38BA8"/>
                                <Run Text="    Safe: "/><Run x:Name="SafeConnectionsRun" Text="0" FontWeight="Bold" Foreground="#A6E3A1"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <DataGrid Grid.Row="1" x:Name="ConnectionsGrid">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Background" Value="#1E1E2E"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSuspicious}" Value="True">
                                        <Setter Property="Background" Value="#2A1F1F"/>
                                        <Setter Property="Foreground" Value="#F38BA8"/>
                                    </DataTrigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#45475A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Status" Binding="{Binding DisplayFlag}" Width="110"/>
                            <DataGridTextColumn Header="Time" Binding="{Binding DisplayTime}" Width="80"/>
                            <DataGridTextColumn Header="Destination" Binding="{Binding DisplayAddress}" Width="350"/>
                            <DataGridTextColumn Header="Port" Binding="{Binding RemotePort}" Width="70"/>
                            <DataGridTextColumn Header="State" Binding="{Binding Status}" Width="100"/>
                            <DataGridTextColumn Header="Reason" Binding="{Binding Reason}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <!-- Scripts tab -->
            <TabItem Header="📜 Local Scripts">
                <Grid Margin="4">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Stats bar -->
                    <Border Grid.Row="0" Background="#313244" CornerRadius="6" Padding="12,8" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Foreground="#A6ADC8" VerticalAlignment="Center">
                                <Run Text="Scripts found: "/><Run x:Name="TotalScriptsRun" Text="0" FontWeight="Bold" Foreground="#CDD6F4"/>
                                <Run Text="    Flagged: "/><Run x:Name="FlaggedScriptsRun" Text="0" FontWeight="Bold" Foreground="#F38BA8"/>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="350"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Script list -->
                        <DataGrid Grid.Column="0" x:Name="ScriptsGrid" SelectionChanged="ScriptsGrid_SelectionChanged" Margin="0,0,4,0">
                            <DataGrid.RowStyle>
                                <Style TargetType="DataGridRow">
                                    <Setter Property="Background" Value="#1E1E2E"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSuspicious}" Value="True">
                                            <Setter Property="Background" Value="#2A1F1F"/>
                                            <Setter Property="Foreground" Value="#F38BA8"/>
                                        </DataTrigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#45475A"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGrid.RowStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Status" Binding="{Binding DisplayFlag}" Width="90"/>
                                <DataGridTextColumn Header="File" Binding="{Binding DisplayName}" Width="160"/>
                                <DataGridTextColumn Header="Size" Binding="{Binding DisplaySize}" Width="70"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <!-- Code viewer -->
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Warnings panel -->
                            <Border x:Name="WarningsPanel" Grid.Row="0" Background="#2A1F1F" CornerRadius="6"
                                    Padding="12,8" Margin="0,0,0,4" Visibility="Collapsed">
                                <StackPanel>
                                    <TextBlock Text="⚠ Warnings:" Foreground="#F38BA8" FontWeight="Bold" Margin="0,0,0,4"/>
                                    <ItemsControl x:Name="WarningsList">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding}" Foreground="#FAB387" Margin="8,2"/>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- Code display -->
                            <TextBox Grid.Row="1" x:Name="CodeViewer"
                                     IsReadOnly="True"
                                     FontFamily="Consolas"
                                     FontSize="12"
                                     Background="#11111B"
                                     Foreground="#CDD6F4"
                                     BorderBrush="#313244"
                                     Padding="12"
                                     TextWrapping="Wrap"
                                     VerticalScrollBarVisibility="Auto"
                                     HorizontalScrollBarVisibility="Auto"
                                     Text="Select a script from the list to view its contents."/>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Status bar -->
        <Border Grid.Row="2" Background="#313244" Padding="12,6">
            <TextBlock x:Name="StatusText" Foreground="#A6ADC8" FontSize="12" Text="Ready — click Start to begin monitoring"/>
        </Border>
    </Grid>
</Window>
